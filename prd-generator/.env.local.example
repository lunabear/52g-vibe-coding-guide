# MISO API Configuration
# Remove the NEXT_PUBLIC_ prefix to keep these values server-side only
MISO_API_KEY=your-miso-api-key-here
MISO_ENDPOINT=your-miso-endpoint-here

# App Configuration (optional)
NEXT_PUBLIC_APP_NAME=PRD Generator
NEXT_PUBLIC_APP_URL=http://localhost:3000
EOF < /dev/null